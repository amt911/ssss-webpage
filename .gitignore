node_modules/
coverage/
playwright-report/
test-results/
.stryker-tmp/
reports/

# Template kit cloned in-tree while bootstrapping this repo. Not a submodule.
claude-md/

# NOTE: dist/ is deliberately NOT ignored — dist/index.html is the shipped
# artifact and must be downloadable straight from the repo without a toolchain.
